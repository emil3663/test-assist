### TA-231 — A spanning selection across screens of unequal height produces an unexplained black band

- **Phase:** 3
- **Priority:** P2
- **Suggested labels:** `ux`, `capture`, `multi-display`
- **Problem it solves:** When two displays have different *logical* heights, the
  virtual desktop is L-shaped rather than rectangular, and a rectangular
  selection spanning both necessarily covers a region that belongs to no
  screen. The export fills that region black. The fill itself is correct — no
  display is there, so there are no pixels to capture — but it arrives with no
  warning and no explanation, and the user is left deciding whether they are
  looking at a defect.

  Measured on the reference rig (laptop 1536x864 @125% at x -1536..0, external
  1920x1080 @100% at x 0..1920, tops aligned), the unmapped region is a
  1536x216 block at the bottom left. A drag from (-1400, 100) to (1800, 1000)
  yields:

  | region | extent | captured |
  |---|---|---|
  | laptop content | x -1400..0, y 100..864 | yes |
  | **unmapped**   | x -1400..0, y 864..1000 | **no display** |
  | external content | x 0..1800, y 100..1000 | yes |

  **Cropping cannot fix this.** Cutting y at 864 discards 136 rows of real
  external content; cutting x at 0 discards the laptop half entirely. The
  captured content is L-shaped, and the tightest rectangle containing an L is
  the full bounding box. Any crop that removes the band removes pixels the user
  deliberately selected.

  **Compositing the gap away is worse and is explicitly rejected.** Butting the
  two pieces together to eliminate the band would place content adjacent that is
  not adjacent on the physical desktop. For a general screenshot utility that is
  a cosmetic convenience; for an evidence-capture tool whose output goes into bug
  reports it produces a falsified artifact. The spatial relationship between
  captured regions is part of the evidence.

  So the band stays. What must change is that it stops being a surprise: the
  user should see the unmapped region *before* releasing the mouse, not discover
  it in the exported file.

  Note that constraining the cursor is not available as a remedy. Windows
  already confines the pointer to real screen area, so both corners of any drag
  are legal positions; the rectangle between them can still span unmapped space.
  Visual feedback during the drag is the only prevention that exists.

- **Scope:**
  - The overlay renders regions of `virtualGeometry()` covered by no screen
    distinctly from selectable area — hatched, or dimmed differently — so the
    unmapped region is visible while the selection is being drawn.
  - **Decide and record** whether the export fills unmapped regions with black
    or with transparency. PNG supports alpha and a transparent band drops into a
    ticket or document more cleanly, but clipboard and JPEG paths flatten it and
    some viewers render transparency as white. Decide it; do not let it default
    by inaction.
  - Update DSP-08's expected result wherever it appears. `DESKTOP_TEST_PLAN.md`'s
    CAP-16 (also labelled DSP-08) reads "no black band" — correct for *its own*
    scenario, a gap *between* two screens that `plan_capture()` already closes
    by packing them adjacently, but not achievable for a spanning drag across
    screens of unequal height, where packing one axis cannot close a real
    mismatch on the other. `MULTI_DISPLAY_MANUAL_PASS.md`'s own DSP-08 row
    ("no gap, no doubling, no vertical offset") is worded broadly enough to
    invite the same misreading — and the reference rig above has exactly this
    height mismatch. Both need the unequal-height case named as its own
    expectation rather than folded into "no gap".
- **Deliverables:**
  - The overlay change, with a test asserting unmapped regions are identified
    correctly for a non-rectangular screen union.
  - A regression test asserting that captured pieces keep their relative offsets
    — the guard against anyone later "fixing" the band by closing the gap.
  - The black-vs-transparent decision, written down whichever way it goes.
  - DSP-08's expectation corrected in `MULTI_DISPLAY_MANUAL_PASS.md` and
    `DESKTOP_TEST_PLAN.md` (not `TEST_PLAN.md`, which is the browser build's
    plan and has no DSP-08 case at all).
- **Acceptance criteria:**
  - With two screens of different logical heights connected, beginning a region
    drag shows any unmapped area visually distinct from selectable area.
  - A spanning capture's unmapped region matches the recorded decision, and the
    pass doc states that expectation rather than contradicting it.
  - A test fails if captured pieces are repositioned relative to one another.
- **Dependencies:** None blocking. Touches the same overlay geometry as the
  v1.4.0 `availableVirtualGeometry()` -> `virtualGeometry()` change (see CAP-21),
  so it is cheaper done alongside further work in that area than separately.
  Not release-blocking — 1.4.0's behaviour is correct, merely unexplained.

## Decision (recorded 2026-09-13, as part of TA-232)

TA-232 was fixed as one `ScreenshotOverlay` window per `QScreen`, each set
to that screen's own geometry (see `docs/TA-232.md`'s Resolution section).
As predicted in that issue's own dependency note above, this **removes
this ticket's own visual-feedback requirement as a side effect, not by
building it separately.**

Under the old single-window design, a gap between mismatched screens was a
region *inside* one window's own rect that had to be explicitly excluded
from the dim (`_covered_region()`), so a mistake there could silently dim
— and invite a selection into — a region no screen occupies. Under the
per-screen-window design there is no such region to exclude: each window's
rect *is* one real screen in full, so a gap between screens is simply not
covered by any window's geometry at all. The user sees it as plain,
undimmed background while dragging — visually distinct from selectable
area, which is exactly this ticket's own acceptance criterion — without a
single line of hatching or gap-detection code. Covered by
`test_the_gap_between_mismatched_screens_is_covered_by_no_window` in
`python/tests/test_capture.py`, which asserts no per-screen window's
geometry contains a point known to belong to neither screen in the
reference rig's own mixed-height layout.

**This ticket is closed as resolved-by-side-effect of TA-232**, not by its
own separate implementation. Nothing further is needed for the overlay
half of its scope.

**The black-vs-transparent export-fill decision, deferred by both this
ticket and `docs/TA-232.md`, is: transparent.** `ScreenshotOverlay._grab()`
already fills the composited result with `Qt.GlobalColor.transparent`
before painting any piece onto it, and pieces are only ever painted for
sub-regions that actually intersect a real screen (`plan_capture()`
excludes anything that does not) — so any part of a composited image a
piece never reaches was already transparent, by construction, before this
fix. Black would have meant filling the canvas opaque first and painting
over it, which is not what the code does and was never proposed as a
change; transparent needed no new code, only naming it as the deliberate
choice it already was. PNG export (this tool's primary format) keeps the
alpha; clipboard and JPEG paths flatten it to whatever the receiving
application decides, same as for any other transparent PNG — an existing,
general limitation of those two paths, not specific to this region.

**Reopened, 2026-09-15 — the transparent-fill decision above was correct in intent but not actually happening on real hardware.** TA-204 hardware verification found two evidence files with a solid *opaque black* rectangle in the unmapped region, not transparency — confirmed via Pillow (`mode RGB`, sampled pixel `(0,0,0,255)`, no alpha channel at all). Suspected mechanism: `ScreenshotOverlay._grab()` built the composite canvas with bare `QPixmap(size)` before filling it `Qt.GlobalColor.transparent` — `QPixmap(size)` does not reliably carry a real alpha channel on every platform/build, so the fill could silently become opaque black instead of true transparency. The reasoning in the "Decision" section above (`pieces are only ever painted for sub-regions that actually intersect a real screen... so any part of a composited image a piece never reaches was already transparent, by construction`) assumed the fill itself was reliable, which is exactly the part that wasn't guaranteed. Fixed in `python/capture.py::_grab()`: the canvas is now built as `QImage(size, QImage.Format.Format_ARGB32_Premultiplied)`, filled transparent, painted into, then converted once via `QPixmap.fromImage()` — a QImage in an explicit ARGB format is documented to always carry a real alpha channel, unlike a bare `QPixmap(size)`, whose format is platform/backend-dependent. Covered by `test_TA232_HW1_unpainted_corner_between_unequal_height_pieces_stays_transparent` in `python/tests/test_functional.py`. See `docs/TA-232-HARDWARE-VERIFICATION.md` for the original hardware evidence (files under HW-1/HW-4).

**Status of that mechanism, 2026-09-15 (later same day) — verified on the deployed build's own hardware, not confirmed to reproduce.** Three direct checks against the actual reference rig (laptop 1536×864@1.25, external 1920×1080@1.0, the same physical machine `TestAssist.exe` was captured on):

1. **Bundled Qt/PySide6 version matches.** `C:\TestAssist\_internal\PySide6\Qt6Core.dll` (the deployed `b77ce50a` build) reports `6.11.0.0` — identical to this dev venv's installed `PySide6` (`6.11.0`). A version-driven default-format difference is ruled out.
2. **Freezing doesn't reproduce it either.** A minimal PyInstaller-frozen probe (same `pyside6.abi3.dll`/`Qt6Core.dll` stack, same machine) still returns real alpha from `QPixmap(size).fill(Qt.GlobalColor.transparent)` — `after_fill_pixel=(0, 0, 0, 0)`, not `(0, 0, 0, 255)`.
3. **The actual pre-fix code, run against real screens with real `grabWindow()` calls on this same rig, doesn't reproduce it.** `git log --oneline b77ce50..1df02f9 -- python/capture.py` confirms the fix commit is the *only* change to `capture.py` between the deployed build and now — so this ran byte-identical canvas-construction code. Reproducing the exact `TA-225`-logged drag (`QRect(1216, 788, 847, 244)`, the same rect that produced one of the two black-rectangle evidence files) yields the same `579x305` output size as the real file, but the unpainted corner samples as `rgba(0, 0, 0, 0)` — real transparency — not opaque black.

So: the hardware evidence itself is not in doubt (two independent files, Pillow-measured, `mode RGB`, no alpha). What's unresolved is *why* — same source, same Qt version, same real hardware, same real capture path, direct or frozen, does not reproduce it here. The fix is kept regardless (an explicit `QImage` alpha format is strictly safer than relying on `QPixmap`'s undocumented default, whatever that default's actual failure trigger turns out to be), but "root cause identified" in the paragraph above should be read as "plausible, evidence-consistent mechanism, fixed defensively" rather than "reproduced and confirmed." Remaining candidate: something about the *conditions* the original capture happened under (e.g. how the desktop session was being driven at that moment) rather than the code, version, or packaging — not yet investigated.

**One caveat, out of scope for TA-232 and not fixed here:** `plan_capture()`'s
own axis-packing heuristic (`screen_geometry.py`, unchanged by TA-232 on
purpose) decides whether to close a gap by packing pieces horizontally or
vertically based on whether their *intersections with the drag rect* share
an x-range. For a drag that touches only a sliver of two screens truly
separated in x, near where their intersections happen to meet at a single
point, that heuristic can misjudge the pair as vertically stacked instead
— producing a composited image with the two pieces placed in unrelated
corners of a much larger canvas, rather than the coherent L-shaped union
with a transparent band this ticket describes. This is a pre-existing
property of `plan_capture()`'s own axis choice, not something TA-232's
window-per-screen port touched or introduced, and reproducing or fixing it
is not attempted here — if it turns out to matter on real hardware, it
needs its own ticket against `screen_geometry.py`, not a reopening of this
one.
