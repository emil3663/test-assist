### TA-236 — Recordings play back in an external player, not inside Test Assist

- **Phase:** 3
- **Priority:** P2
- **Suggested labels:** `enhancement`, `capture`, `ux`
- **Problem it solves:** Reported in today's Block A findings: opening a saved
  recording launches the system default video player (VLC, on the reporter's
  machine) rather than playing it inside Test Assist's own editor. DSP-10
  itself passed — the recorded content is correct — this is about where
  playback happens, not what was recorded.
- **Scope:** Decide explicitly whether in-app playback is in scope now or a
  deferred enhancement (this project's convention, per TA-224, is to record
  that decision rather than let it default by inaction). If building it,
  scope an embedded video player in the editor for recording files, distinct
  from the still-image editor path.
- **Deliverables:** A recorded decision either way; if in scope, in-app
  playback for recordings opened from history.
- **Acceptance criteria:** N/A until the scope decision is made.
- **Dependencies:** None blocking.
